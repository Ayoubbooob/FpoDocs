import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';
import 'form.dart';

class Formescreen extends StatelessWidget {
  const Formescreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Forme(),
    );
  }
}